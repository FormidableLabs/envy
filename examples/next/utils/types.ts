export type CatFact = { _id: string; text: string };
export type Cocktail = {
  id: string;
  name: string;
  ingredients: string[];
  instructions: string[];
  imageUrl: string;
};
export type Dog = {
  imageUrl: string;
};
