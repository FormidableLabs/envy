name: Pull Request
on:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: yarn lint

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: yarn build

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: yarn test
